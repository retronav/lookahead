import React from "react";
export const Offline = ({ children }: { children: JSX.Element }) => {
  // TODO: render when offline
};
export const Online = ({ children }: { children: JSX.Element }) => {
  // TODO: render when online
};
